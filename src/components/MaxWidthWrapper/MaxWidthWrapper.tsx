"use client";

import { styled } from "styled-components";

const MaxWidthWrapper = styled.div`
  padding-left: 32px;
  padding-right: 32px;
  margin-left: auto;
  margin-right: auto;
`;

export default MaxWidthWrapper;
